# 버서스 파이어베이스 설정 안내

데이터베이스 주소: `https://tipp7versus-default-rtdb.firebaseio.com/`

버서스의 모든 데이터는 전용 경로인 `versus-v1`에 저장됩니다. 카멜레온의 데이터베이스나 규칙은 사용하지 않습니다.

## 실제 서버 연결하기

1. 파이어베이스 콘솔에서 `tipp7versus` 프로젝트를 선택하고 **인증 → 로그인 방법 → 익명**을 활성화합니다.
2. 제공된 **웹 API 키**는 모드에 기본값으로 등록되어 있으므로 게임에서 따로 입력할 필요가 없습니다. 키가 바뀌면 **프로젝트 설정 → 일반**에서 새 웹 API 키를 확인하고 게임의 **지오드 → 버서스 → 설정 → 서버 API 키 입력란**에 넣습니다. 이 값은 클라이언트 설정용 키입니다. 서비스 계정 키나 데이터베이스 비밀 키 같은 관리자 자격 증명은 넣지 마세요.
3. **실시간 데이터베이스 → 규칙**에 이 폴더의 `firebase-rules.json` 내용을 붙여넣고 게시합니다. 명령줄 도구를 사용하는 경우, 이 폴더에서 프로젝트 소유자 계정으로 로그인한 뒤 아래 명령을 실행해도 됩니다.

   ```powershell
   firebase login
   firebase deploy --only database --project tipp7versus
   ```

데이터베이스 주소만으로는 익명 인증을 활성화할 수 없습니다.

제공된 규칙은 버서스 전용 데이터베이스의 기존 규칙을 대체하며, 지정하지 않은 경로의 접근을 기본적으로 차단합니다. 다른 앱과 데이터베이스를 공유한다면 해당 앱의 접근 규칙을 먼저 병합해야 합니다.

## 데이터 구조와 접근 권한

| `versus-v1` 아래의 경로 | 저장 내용 | 클라이언트 접근 권한 |
| --- | --- | --- |
| `rooms/{roomId}` | 방, 참가자, 맵, 게임 규칙, 준비 상태, 이모티콘, 경기 진행과 승패 | 인증된 사용자 조회 가능. 각 참가자는 본인 상태만 변경 가능 |
| `rooms/{roomId}/launch` | 시작 식별자, 요청 시각, 두 플레이어의 로딩 완료 여부, 공통 카운트다운 기준 시각 | 방장만 생성. 본인의 로딩 완료 상태만 변경 가능. 둘 다 로딩한 뒤 기준 시각 한 번 확정 |
| `roomSecrets/{roomId}` | 방장 식별자와 네 자리 비밀번호. 빈 비밀번호는 공개방을 의미 | 저장된 비밀번호는 방장만 조회 가능. 방이 닫히기 전까지 변경 불가 |
| `admissions/{roomId}/{uid}` | 참가자가 방 비밀번호를 알고 있다는 확인 정보 | 올바른 비밀번호 제출만 허용. 클라이언트 조회 불가 |
| `history/{uid}/{matchId}` | 확정된 경기 전적 | 본인의 최근 열 경기만 조회 가능. 방에 확정된 결과와 정확히 일치할 때만 작성 가능 |

방은 방장 한 명과 참가자 최대 한 명으로 구성됩니다. 입장, 퇴장, 맵 선택, 접속 유지 요청은 서버 데이터의 버전 표식을 비교하여 다른 요청과 충돌하지 않을 때만 반영합니다. 보안 규칙은 참가자가 방장 정보, 선택한 맵, 시작 상태를 임의로 바꾸지 못하게 합니다.

비밀방 비밀번호는 방 목록 응답에 포함되지 않습니다. 비밀번호는 숫자 네 자리이며, 앞자리가 영인 경우도 허용합니다.

아직 생성되지 않은 비밀번호 경로는 인증된 사용자가 조회하면 `null`을 받을 수 있습니다. 이는 최초 생성 요청의 `If-Match: null_etag` 조건 검사에 필요합니다. 비밀번호가 저장된 뒤에는 방장만 조회할 수 있으며, 비밀번호 목록 전체 조회는 계속 차단됩니다.

방 목록은 십 초마다 갱신하며, 최근 갱신된 방을 최대 이백 개까지 가져옵니다. 방 내부 정보는 평소 이 초마다, 시작 요청이 진행되는 동안에는 반 초마다 동기화합니다. 접속 유지 신호는 십 초마다 보냅니다. 마지막 접속 유지 시각부터 사십오 초가 지나면 접속이 만료된 것으로 처리합니다.

접속이 만료된 방장의 방은 목록에서 제외됩니다. 참가자의 접속이 만료되면 방장이 해당 자리를 비울 수 있습니다. 방 삭제가 확인되면 참가자는 방 목록으로 돌아가고 방장이 나갔다는 안내가 표시됩니다. 통신 오류는 방 삭제와 구분하여 재시도하거나 연결 끊김으로 처리합니다.

익명 인증 계정은 갱신 토큰과 함께 모드의 로컬 저장 데이터에 유지됩니다. 방에 표시되는 아이콘, 지오메트리 대시 이름, 승률은 참가자가 전달한 표시용 정보입니다. 파이어베이스의 실제 소유권 판단에는 표시 이름이나 게임 계정 번호가 아닌 `auth.uid`를 사용합니다.

## 현재 구현 범위

두 플레이어는 선택한 맵과 음악·효과음을 모두 내려받은 뒤 각자 **준비 버튼**을 누릅니다. 방장이 맵이나 게임 규칙을 바꾸면 양쪽 준비가 해제됩니다. 두 명 모두 준비한 뒤에만 방장이 경기를 시작할 수 있습니다.

시작 요청마다 새로운 식별자와 서버 시각을 기록합니다. 두 클라이언트는 선택한 맵을 내려받고 기본 플레이 화면을 불러온 뒤, 각자 자신의 로딩 완료 상태를 기록합니다. 로딩 중에는 게임 진행을 멈추며, 두 명 모두 완료하면 서버에 공통 기준 시각을 한 번 기록합니다. 두 클라이언트는 그 시각으로부터 **삼 초 뒤**에 카운트다운을 마치고 각자의 게임을 시작합니다. 네트워크 지연과 프레임 차이에 따라 실제 화면의 시작 시각에는 차이가 있을 수 있습니다.

로딩 제한은 요청 시각부터 **육십 초**입니다. 제한 초과, 다운로드 실패, 취소·퇴장 시 시작 정보와 양쪽 준비 상태를 함께 지웁니다. 보안 규칙은 타인의 로딩 완료 위조, 너무 이른 카운트다운, 육십 초 뒤 완료 보고와 이전 요청 재사용을 거부합니다.

게임 규칙은 어템 모드, 퍼센트 모드, 프렉티스 모드와 어템 시퀀스를 지원합니다. 어템 모드는 양쪽의 최고 진행률이나 프렉티스 어템 수로 승패를 정합니다. 시퀀스에서는 서버가 정한 첫 플레이어부터 차례로 진행합니다. 퍼센트 모드는 먼저 목표에 도달한 뒤 상대가 진행 중이던 같은 어템까지 기다려 승리 또는 무승부를 정합니다.

상대가 관전을 시작하기 전에는 위치와 카메라 정보를 전송하지 않습니다. 관전 중에만 약 초당 일곱 번 전송합니다. 퍼센트 모드에서는 위치를 전송하지 않습니다. 일시정지 상태, 진행률, 남은 어템과 기권 상태는 방의 경기 상태에 기록됩니다. 일시정지가 삼십 초를 넘으면 패배로 확정할 수 있습니다.

방 이모티콘은 사용자별로 일 초에 한 번만 전송할 수 있습니다. 경기 결과는 두 참가자의 진행 상태와 설정 규칙으로 계산되며, 임의의 승자나 전적을 기록하는 요청은 거부됩니다. 클라이언트가 보내는 진행률 자체를 독립적으로 재현하는 전용 판정 서버는 없으므로 변조된 게임 클라이언트까지 막는 구조는 아닙니다.

이번 버전은 모든 방에 양쪽 준비, 게임 규칙, 이모티콘 시각과 경기 상태 필드가 필요합니다. 이전 버전의 방은 닫고 새로 생성합니다. 모드 업데이트와 함께 이 폴더의 최신 규칙도 반드시 게시해야 합니다.

최근 전적 창과 최근 열 경기 승률은 서버에 저장된 실제 경기 결과를 읽습니다. 새 프로젝트에는 전적이 없으므로 목록은 비어 있고 승률은 `--`로 표시됩니다.

경기가 끝나면 모드는 방에 확정된 결과와 일치하는 전적만 기록합니다. 전적 한 건의 형식은 다음과 같습니다.

```json
{
  "roomId": "방 식별자",
  "opponentName": "상대 이름",
  "levelName": "맵 이름",
  "winnerName": "승자 이름",
  "result": "win",
  "playedAt": 1789700000000
}
```

`result`에는 해당 플레이어 기준으로 승리인 `win`, 패배인 `loss`, 무승부인 `draw` 중 하나를 저장합니다. `playedAt`은 밀리초 단위 서버 시각입니다. 각 플레이어의 식별자 아래에 해당 플레이어 관점의 전적을 각각 저장합니다. 클라이언트는 가장 최근 열 건을 조회하여 승률을 계산합니다.

오래된 방과 비밀번호 정보의 정리, 비밀번호 입력 시도 횟수 제한도 추후 서버에서 처리할 수 있습니다. 네 자리 방 비밀번호는 간단한 입장 제한 용도입니다.

## 보안 규칙 검증하기

검사는 로컬 주소인 `127.0.0.1`, 별도 시험용 경로인 `demo-versus-rules`, 에뮬레이터 전용 가상 계정만 사용합니다. 실제 데이터베이스는 변경하지 않습니다.

명령줄 도구로 데이터베이스 에뮬레이터를 실행합니다.

```powershell
firebase emulators:start --only database --project demo-versus-rules
```

에뮬레이터를 켜 둔 상태에서 다른 터미널을 열고, 이 폴더에서 검사 스크립트를 실행합니다.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tests/firebase-rules.ps1
```

공식 독립 실행형 에뮬레이터 파일을 사용하는 경우에는 다음 명령으로 실행해도 됩니다.

```powershell
java -jar build-tools/firebase-database-emulator.jar --host 127.0.0.1 --port 9000
```

검사 항목에는 미인증 접근 차단, 비밀방 보호, 두 명 정원, 동시 입장, 준비와 로딩 권한, 게임 규칙, 이모티콘 속도 제한, 위치 전송 제한, 어템·퍼센트·시퀀스·프렉티스 판정, 전적 위조 차단과 목록 상한이 포함됩니다. 내려받은 에뮬레이터와 빌드 파일은 모드 리소스에 포함되지 않습니다.

참고 문서: [파이어베이스 보안 규칙](https://firebase.google.com/docs/reference/security/database?hl=ko), [조건부 데이터 저장 요청](https://firebase.google.com/docs/database/rest/save-data?hl=ko#section-conditional-requests), [익명 인증](https://firebase.google.com/docs/auth/web/anonymous-auth?hl=ko).


## 전적 상세와 경기 후 방 복귀

최신 `firebase-rules.json` 전체를 데이터베이스 규칙에 붙여 넣고 게시해야 합니다.

- 전적에 두 플레이어의 이름·아이콘·최고 퍼센트·경기 규칙·맵 번호를 저장합니다.
- 각 어템의 최고 퍼센트는 `matchAttempts` 아래에 따로 저장합니다. 방 목록과 진행 상황을 받을 때 어템 기록 전체를 반복해서 내려받지 않습니다.
- 상세 창에서는 어템 기록을 10개씩 조회합니다. 본인과 해당 경기의 상대만 기록을 조회할 수 있습니다.
- 결과 연출이 끝나면 전적 저장 후 방으로 복귀합니다. 양쪽이 결과를 확인하면 준비 상태를 해제하고 같은 방에서 다음 경기를 준비합니다.
- 업데이트 전 전적에는 상세 기록이 없으므로 기존 요약만 표시합니다.
- 관전자 미니게임은 기기 안에서만 동작하며 경기 승패와 어템 수에 영향을 주지 않습니다. 상대 진행 상황만 받아오며 위치 전송은 사용하지 않습니다.
