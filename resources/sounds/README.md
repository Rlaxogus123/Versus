# Versus custom sound slots

Drop the requested OGG files in this directory. The mod automatically prefers
them and keeps a built-in Geometry Dash sound as fallback when a file is absent.

See `SOUND_REQUESTS.md` in the project root for names and timing.
