cmake_minimum_required(VERSION 3.21)
project(VersusMembershipTests LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(VERSUS_DEPS "${CMAKE_CURRENT_SOURCE_DIR}/../build/_deps" CACHE PATH "Existing Geode build dependencies")
add_subdirectory("${VERSUS_DEPS}/result-src" result)
add_subdirectory("${VERSUS_DEPS}/json-src" json)
add_executable(membership membership.cpp)
target_include_directories(membership PRIVATE ../src)
target_link_libraries(membership PRIVATE mat-json)
enable_testing()
add_test(NAME membership COMMAND membership)
