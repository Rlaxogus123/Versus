# Firebase 사용량 예상: 100개 방을 1시간 동시 운영

작성 기준일: 2026-09-19

## 결론

100개 방에 각각 2명이 접속하여 1시간 동안 실제 경기를 진행하면 현재 구현 기준 Firebase Realtime Database 다운로드는 다음 정도로 예상된다.

| 경기 상태 | 예상 과금 다운로드 | Blaze 예상 비용 |
|---|---:|---:|
| 양쪽 동시 플레이, 관전 없음 | 약 10.2~12.5 GB | 약 $9.8~12.2 |
| 경기 시간의 절반이 관전 전송 상태 | 약 12.7~15.5 GB | 약 $12.3~15.2 |
| 한쪽이 계속 관전하여 위치를 7 Hz로 전송 | 약 15.2~18.6 GB | 약 $14.8~18.2 |
| 7 Hz 전송 뒤 즉시 방 조회까지 매번 발생하는 코드상 상한 | 약 19.3~23.6 GB | 약 $19.0~23.3 |

운영 예산은 **100개 경기 방당 시간당 25 GB, 약 $25**로 잡는 것이 안전하다. CAS 충돌 재시도, 긴 UTF-8 이름, 보안 규칙 거부 요청, 연결 재수립은 별도 여유분이다.

현재 REST 연결이 재사용되지 않아 요청마다 TLS 연결을 새로 맺는다면 7 Hz 관전 구간은 약 **38~49 GB/시간**까지 증가할 수 있다. 실제 배포 전에 10분 부하 시험으로 확인해야 한다.

## 계산 조건

- 방: 100개
- 인원: 방당 2명, 총 200명
- 시간: 3,600초
- 경기 중 방 조회: 플레이어당 0.5초마다 1회
- 대기방 조회: 플레이어당 2초마다 1회
- 생존 신호: 플레이어당 10초마다 1회
- 일반 진행 상황 보고: 값이 변하는 동안 플레이어당 최대 0.5초마다 1회
- 관전용 위치 보고: 실제 플레이어 1명이 초당 7회
- 일반 활성 경기 방 JSON: 약 1,780바이트
- 대기방 JSON: 약 862바이트
- HTTP 헤더와 재사용된 TLS 레코드 오버헤드: 응답당 0.5~1 KiB 가정
- CAS 재시도와 경기 시작·종료의 일회성 요청은 표의 기본값에서 제외
- 단위는 10진 GB 기준

실제 방 JSON은 이름과 좌표 길이에 따라 달라진다. 현재 스키마의 보통 값은 약 1.3~1.8 KB이며, 허용된 최대 길이와 문자 이스케이프를 모두 적용한 보수적 상한은 약 4.64 KB다.

## 현재 코드의 요청 구조

현재 방 변경은 작은 필드만 갱신하지 않는다. 논리 변경 1회마다 다음 과정이 실행된다.

1. 방 전체를 `GET`하고 ETag를 받는다.
2. 수정한 방 전체를 `PUT`한다.
3. `PUT` 응답으로 방 전체 JSON을 다시 받는다.
4. ETag가 충돌하면 최대 3회 재시도한다.

따라서 논리 변경 1회는 정상 처리 시 전체 방 응답 2회와 전체 방 업로드 1회를 만든다. `PUT`에 `print=silent=true`가 없으며, 응답의 `updatedAt`을 읽기 때문에 쓰기 응답도 다운로드 과금에 포함된다.

관련 코드:

- REST 요청: `src/VersusService.cpp:321-352`
- 전체 방 CAS 변경: `src/VersusService.cpp:417-442`
- 경기 중 0.5초 조회와 10초 생존 신호: `src/VersusService.cpp:1025-1066`
- 진행 상황 전체 방 변경: `src/VersusService.cpp:949-1013`
- 일반 2 Hz 및 관전 7 Hz 보고: `src/BattleSession.cpp:192-208`, `src/BattleSession.cpp:373-378`

## 요청 횟수

| 상태 | GET | PUT | 총 HTTP 요청 | 초당 평균 |
|---|---:|---:|---:|---:|
| 대기방 100개 | 432,000 | 72,000 | 504,000 | 140 |
| 경기 시작, 진행 보고 없음 | 1,512,000 | 72,000 | 1,584,000 | 440 |
| 양쪽 일반 진행 보고 2 Hz | 2,952,000 | 1,512,000 | 4,464,000 | 1,240 |
| 경기 시간의 절반이 7 Hz 관전 상태 | 3,492,000 | 2,052,000 | 5,544,000 | 1,540 |
| 전 시간 7 Hz 관전 상태 | 4,032,000 | 2,592,000 | 6,624,000 | 1,840 |
| 7 Hz 보고마다 즉시 조회까지 발생 | 5,832,000 | 2,592,000 | 8,424,000 | 2,340 |

일반 진행 보고는 `dirty`일 때만 전송된다. 플레이 중 퍼센트가 계속 바뀌면 2 Hz에 가깝고, 정지 상태에서는 줄어든다. 관전 보고는 강제 전송이므로 네트워크 왕복이 충분히 빠르면 7 Hz에 가깝다.

두 플레이어가 동시에 같은 ETag로 `PUT`하면 한쪽은 412 충돌 후 다시 `GET + PUT`한다. 완전히 동기화된 일반 2 Hz 보고에서 매번 한쪽이 한 번 재시도하면 총 요청은 약 4.46백만 회에서 약 5.98백만 회로 늘어난다.

## 다운로드 산출

대표 방 크기 1,780바이트를 적용한 순수 JSON 응답량은 다음과 같다.

| 관전 전송 시간 비율 | 전체 방 응답 수 | JSON 응답량 | 프로토콜 포함 예상량 |
|---:|---:|---:|---:|
| 0% | 4,464,000 | 7.95 GB | 10.2~12.5 GB |
| 50% | 5,544,000 | 9.87 GB | 12.7~15.5 GB |
| 100% | 6,624,000 | 11.79 GB | 15.2~18.6 GB |
| 100%와 보고 후 즉시 조회 | 8,424,000 | 14.99 GB | 19.3~23.6 GB |

Firebase는 JSON 본문만 과금하지 않는다. HTTP 헤더, Realtime Database 프로토콜, TLS 암호화 오버헤드와 최초 TLS 핸드셰이크도 다운로드에 포함한다. 공식 문서는 최초 TLS 핸드셰이크를 평균 약 3.5 KB로 안내한다. 위 표는 연결 재사용을 전제로 응답당 0.5~1 KiB를 더한 예상치다.

참고로 `PUT` 업로드 본문은 관전 비율 0%/50%/100%에서 각각 약 2.69/3.65/4.61 GB다. 이는 위 다운로드 비용에는 포함하지 않았지만 네트워크와 서버 부하에는 영향을 준다.

## 대기방과 방 목록

경기를 시작하지 않고 두 명이 방에서 1시간 대기하면 약 504,000회 요청과 **약 0.7~1.0 GB** 다운로드가 예상된다.

100개 활성 방 목록 1회의 대표 JSON 크기는 약 182 KB다. 별도의 사용자가 로비를 계속 열어 10초마다 갱신하면 사용자 1명당 약 65 MB/시간이 추가된다.

| 로비 상시 접속자 | 방 목록 추가 다운로드 |
|---:|---:|
| 1명 | 약 0.065 GB/시간 |
| 100명 | 약 6.5 GB/시간 |
| 200명 | 약 13.1 GB/시간 |

경기 참가자는 경기 화면에서 방 목록을 반복 조회하지 않으므로 기본 경기 계산에는 이 값을 넣지 않았다.

## 저장 공간

- 활성 방 100개: 약 0.18 MB
- 100개 경기가 한 번 종료될 때 전적 200개: 원시 JSON 약 0.035 MB
- 100개 방이 시간당 한 경기씩 30일 계속 운영될 때 전적 144,000개: 원시 JSON 약 25 MB, 키와 내부 오버헤드를 포함하면 대략 30~50 MB

방 데이터는 덮어쓰기 때문에 누적되지 않는다. 전적은 현재 자동 삭제하지 않으므로 장기 저장량은 계속 증가한다. 이 부하에서는 다운로드 비용이 저장 비용보다 훨씬 크다.

## Firebase 요금과 한도

현재 공식 가격표의 Blaze 기준은 다음과 같다.

- 다운로드: 하루 360 MB 무료 이후 $1/GB
- 저장: 1 GB 무료 이후 $5/GB-월
- 동시 실시간 연결: 데이터베이스당 200,000개

위 비용은 같은 날 다른 사용량이 없는 Blaze 프로젝트에서 360 MB를 공제한 값이며 세금과 환율은 제외했다. 공식 가격표는 360 MB/일로 표시하지만 일부 Firebase 청구 설명 문서에는 여전히 10 GB/월이라는 표현이 남아 있다. 이 문서는 최신 가격표의 일일 기준을 우선 적용했다.

현재 구현은 짧은 REST 요청이므로 REST 요청 자체는 동시 실시간 연결 수에 포함되지 않는다. 이후 WebSocket 또는 SSE 상시 연결로 바꾸어 플레이어당 연결 1개를 사용하면 총 200개가 되어 Spark의 100개 한도를 초과한다. 현재 구조도 무료 다운로드 한도를 빠르게 넘으므로 실제 운영에는 Blaze가 필요하다.

Realtime Database는 읽기·쓰기 횟수별 요금을 별도로 청구하지 않고 저장량과 외부로 전송한 바이트를 주로 청구한다. 요청 횟수는 비용보다 성능과 연결 오버헤드에 영향을 준다.

## 비용을 가장 크게 줄이는 변경

1. 0.5초 전체 방 REST 조회를 지속 연결 또는 SSE로 바꾼다.
2. 진행 상황과 좌표를 별도 경로의 작은 `PATCH`로 전송한다.
3. 전체 방 `PUT` 응답에 `print=silent=true`를 사용하고 시간 동기화는 저빈도 요청으로 분리한다.
4. 7 Hz 보고 성공 직후의 강제 전체 방 조회를 제거한다.
5. 두 플레이어의 보고 시점을 분산하여 ETag 충돌을 줄인다.
6. 오래된 전적을 주기적으로 정리한다.

`print=silent=true`만 적용해도 성공한 `PUT`의 전체 방 응답이 사라지므로, 100개 방 기준 시간당 약 2.7~4.6 GB의 JSON 응답과 해당 프로토콜 오버헤드를 줄일 수 있다.

## 실측 방법

정확한 청구량은 Firebase 콘솔 사용량 화면 또는 Cloud Monitoring의 다음 지표로 확인한다.

- `network/sent_payload_bytes_count`: 응답 데이터 본문
- `network/sent_payload_and_protocol_bytes_count`: 본문과 프로토콜
- `network/sent_bytes_count`: 암호화 오버헤드까지 포함한 청구량에 가장 가까운 값

100개 테스트 방을 10분 운영한 뒤 `sent_bytes_count` 증가량에 6을 곱하면 현재 클라이언트와 실제 네트워크 재사용률을 반영한 1시간 예상값을 얻을 수 있다.

## 공식 자료

- [Firebase 가격표](https://firebase.google.com/pricing)
- [Realtime Database 청구 계산](https://firebase.google.com/docs/database/usage/billing)
- [Realtime Database 사용량 한도](https://firebase.google.com/docs/database/usage/limits)
- [Realtime Database 사용량 모니터링](https://firebase.google.com/docs/database/usage/monitor-usage)
- [Realtime Database 성능 최적화](https://firebase.google.com/docs/database/usage/optimize)
